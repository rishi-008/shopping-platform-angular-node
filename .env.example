# Used by docker-compose.yml
DB_NAME=osp_db
# Note: db/init.sql seeds `osp_db`. If you change DB_NAME, update db/init.sql too.
DB_USER=osp_user
DB_PASSWORD=change_me
DB_ROOT_PASSWORD=change_me_root

API_PORT=3001
CORS_ORIGIN=http://localhost:4200

# Legacy PHP app
PHP_PORT=8081
GOOGLE_MAPS_API_KEY=
